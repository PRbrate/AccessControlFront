export interface PostEventProps {
  name: string;
  eventDate: Date;
  quantParticipants: number;
  // avaliable: boolean;
  // image: string;
  description: string;
  adress: string;
  city: string;
  state: string;
  postalCode: string
}